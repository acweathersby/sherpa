name: test

on: push

env: 
  CARGO_TERM_COLOR_ALWAYS

jobs:
  build:

    runs-on: debian-latest

    step:
      - uses: actions/checkout@v3
      - name: Build 
      - run: ./scripts/build_bytecode
      - name: Test
      - run: ./scripts/run_tests 


  
