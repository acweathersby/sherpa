
function cargo-build {
    if [[ $2 = "dev" ]]
    then
        mkdir -p ./bin
        cargo build --bin $1 && cp ./target/debug/$1 "./bin/$1-dev"
    else
        mkdir -p ./bin
        cargo build --release --bin $1 && cp ./target/release/$1 ./bin
    fi
}