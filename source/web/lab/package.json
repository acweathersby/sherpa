{
    "name": "sherpa-lab",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "build": "npx esbuild src/index.ts --bundle --outdir=public --format=esm",
        "watch": "npx esbuild src/index.ts --bundle --outdir=public --format=esm --watch"
    },
    "dependencies": {
        "codemirror": "^6.0.1"
    },
    "devDependencies": {
        "esbuild": "0.17.10",
        "typescript": "^4.9.3"
    }
}